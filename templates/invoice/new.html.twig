{% extends 'base.html.twig' %}

{% block title %}New Invoice{% endblock %}

{% block body %}
<div class="flex flex-col gap-8 py-8 px-50 h-screen overflow-scroll">
    <div class="text-(--gray) flex gap-4 items-center">
        <i class="fa-solid fa-arrow-left"></i>
        <a href="{{ path('app_invoice_index') }}">Retour au factures</a>
    </div>
    <h1 class="font-bold text-2xl">Créer une facture</h1>

    {{ include('invoice/_form.html.twig') }}

</div>

{% endblock %}